#!/bin/bash

set -euo pipefail

#TODO: replace realwith as it is not a standard utility
OZYDIR="$(
  dirname $(realpath "$0")
)/.."

export PYTHONPATH="${OZYDIR}"
make --quiet -C "${OZYDIR}" deps
exec "${OZYDIR}/.venv/bin/python3.7" \
    "${OZYDIR}/ozy/__main__.py" "$0" "$@"
